---
import LInikButton from "./LInikButton.astro";
import Astroicon from "./icons/Astroicon.astro";
import Githubicon from "./icons/Githubicon.astro";
import Tailwindicon from "./icons/Tailwindicon.astro";
import Wwwicon from "./icons/Wwwicon.astro";
import JavaScripticon from "./icons/Javascripticon.astro";
import Demoicon from "./icons/Demoicon.astro";

const TAGS = {
  ASTRO: {
    name: "Astro",
    class: " bg-black text-white solid border-2 border-white/10",
    icon: Astroicon,
  },
  TAILWIND: {
    name: "Tailwind",
    class: "bg-[#003159]/60 text-white solid border-2 border-white/10 hover:",
    icon: Tailwindicon,
  },
  JAVASCRIPT: {
    name: "JavaScript",
    class: " bg-black text-white solid border-2 border-white/10",
    icon: JavaScripticon,
  },
};

const PROJECTS = [
  {
    title: "Clon Tesla- Landing page",
    description:
      "Un trabajo para aprender hecho junto con midudev, aprender y hacer proyectos junto con el es como ir a un evento artisitco al terminar y ver el resultado que se obtiene con trabjar con tecnologias nuevas y buens practicas",
    image: "/proyects/tesla.webp",
    link: "",
    github: "https://github.com/AlanSan1195/tesla-landing",
    tags: [TAGS.ASTRO, TAGS.TAILWIND],
    demo: "",
  },
  {
    title: "Arkanoid Game - Old School",
    description:
      "Este juego es el primero que desarrollo en mi vida y fue muy divertido hacerlo, junto con Midudev y su comunidad. Aprendí cosas interesantes como el uso del Canvas y el dibujo de los objetos con contextos, también el reinicio de frames y de página. Yo me llevo eso de este tipo de proyectos. Mi juego, por ahora, cuenta con un grado de dificultad interesante desde el principio. ¡Te reto a ROMPER 13 bloques!",
    image: "/proyects/arkanoid.webp",
    link: "",
    github:
      "https://github.com/AlanSan1195/JavaScript-Proyects/tree/main/01%20-arkanold-Game",
    tags: [TAGS.JAVASCRIPT],
    demo: "https://arkanoidsan.netlify.app/",
  },
];
---

<div class="flex flex-col gap-y-16">
  {
    PROJECTS.map(({ image, title, description, tags, link, github, demo }) => (
      <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
        <div class="w-full md:w-1/2">
          <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
            <img
              alt="Recién llegado vs 5 años en Nueva Zelanda"
              class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105"
              loading="lazy"
              src={image}
            />
          </div>
        </div>

        <div class="w-full md:w-1/2 md:max-w-lg">
          <h3 class="text-2xl font-bol text-gray-100 font-semibold">{title}</h3>
          <div class="flex flex-wrap mt-2">
            <ul class="flex flex-row mb-2 gap-x-2">
              {tags.map((tag) => (
                <li>
                  <span
                    class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2 `}
                  >
                    <tag.icon class="size-4" />
                    {tag.name}
                  </span>
                </li>
              ))}
            </ul>

            <div class="mt-2 text-gray-100 dark:text-gray-400">
              {description}
            </div>
            <footer class="flex items-end justify-start mt-4 gap-x-4">
              {github && (
                <LInikButton href={github}>
                  <Githubicon class="size-6" />
                  Code
                </LInikButton>
              )}
              {link && (
                <LInikButton href={link}>
                  <Wwwicon class="size-4" />
                  Preview
                </LInikButton>
              )}

              {demo && (
                <LInikButton href={demo}>
                  <Demoicon class="size-6" />
                  Demo
                </LInikButton>
              )}
            </footer>
          </div>
        </div>
      </article>
    ))
  }
</div>

<!-- esta forma d eabajo es la primera idea  -->

<!-- <section class="flex flex-col gap-y-1">
  {
    PROJECTS.map(({ image, title, description, link, github, tags }) => (
      <>
        <article>
          <h3 class=" text-2xl font-semibold text-yellow-200 mb-2">{title}</h3>
          <p class=" text-lg mb-4 text-pretty">{description}</p>
          <ul class="flex flex-row gap-x-2">
            {tags.map((tag) => (
              <li class=" mb-2">
                <span
                  class={` flex gap-x-2 rounded-full text-xs py-1 px-2 ${tag.class}  items-center `}
                >
                  <tag.icon class=" size-8" />
                  {tag.name}
                </span>
              </li>
            ))}
          </ul>
        </article>
        <img
          class="rounded shadow-2xl shadow-white/10"
          src={image}
          alt={`captura de pantalla del proyecto ${image}`}
        />
        <foote class=" flex  gap-x-2 mt-4 justify-end">
          <LInikButton href={github} class="bg-black text-white ">
            <Githubicon class="size-6" />
            Ver Codigo fuente
          </LInikButton>


          
          {link && (
            // renderizado condicional de un boton 
            <LInikButton>
              <Wwwicon class="size-6" href={link} />
              Ir a pagina web
            </LInikButton>
          )}
        </foote>
      </>
    ))
  }
</section> -->
